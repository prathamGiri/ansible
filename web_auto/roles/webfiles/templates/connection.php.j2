<?php
    //assigne variables
    $servername = '{{ hostvars[groups["dbservers"][0]].inventory_hostname }}';
    $username = '{{ admin_name }}';
    $password = '{{ admin_pass }}';
    $database = '{{ database_name }}';

    //create connection
    $conn = mysqli_connect($servername, $username, $password, $database);

    //check connection
    if (!$conn) {
        //if connection failed die
        die("connection failed".mysqli_connect_error());
    }
?>